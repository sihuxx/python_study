# 01 기본 문법

---

## 1. 변수

```python
x = 10              # 정수 int
y = 3.14            # 실수 float
s = "hello"         # 문자열 str
b = True            # 참/거짓 bool
n = None            # 값 없음

a = b = 0           # 여러 변수 한 번에
a, b = 1, 2         # 각각 다른 값
a, b = b, a         # 값 교환
```

- 타입을 적지 않습니다
- 문장 끝에 세미콜론 없습니다
- **들여쓰기(스페이스 4칸)로 블록을 구분합니다**

---

## 2. 형 변환

```python
int("10")           # 10
int(3.9)            # 3      (버림)
float("3.14")       # 3.14
str(10)             # "10"
list("abc")         # ['a','b','c']
bool(0)             # False
```

---

## 3. 연산자

```python
+   -   *           # 더하기 빼기 곱하기
/                   # 나누기      7 / 2  = 3.5
//                  # 몫          7 // 2 = 3
%                   # 나머지      7 % 2  = 1
**                  # 제곱        2 ** 3 = 8

x += 1              # x = x + 1   (x++ 는 없음)
x -= 1   x *= 2   x /= 2
```

**비교**

```python
==   !=   >   <   >=   <=
```

**논리**

```python
and     # 둘 다 참
or      # 하나라도 참
not     # 반대
```

**포함**

```python
"a" in "abc"        # True
3 in [1, 2, 3]      # True
"x" not in lst      # True
```

---

## 4. 입출력

```python
print("안녕")
print("a", "b")               # a b
print("a", "b", sep="-")      # a-b
print("줄바꿈없이", end="")

name = input("이름: ")         # 항상 문자열로 들어옴
age = int(input("나이: "))     # 숫자로 쓰려면 변환 필요
```

---

## 5. 문자열 포맷 (f-string)

```python
name = "김"
score = 95.567

f"{name}님 {score}점"       # 김님 95.567점
f"{score:.1f}"              # 95.6        소수 1자리
f"{score:.0f}"              # 96          정수로 반올림
f"{1000000:,}"              # 1,000,000   천단위 콤마
f"{name:>10}"               # 오른쪽 정렬
f"{name:<10}"               # 왼쪽 정렬
f"{name:^10}"               # 가운데 정렬
```

---

## 6. 조건문

```python
if x > 10:
    print("큼")
elif x > 5:
    print("보통")
else:
    print("작음")
```

```python
if 0 < x < 100:                 # 범위 비교를 한 줄로
    ...

if x > 0 and y > 0:             # 조건 여러 개
    ...

result = "짝수" if x % 2 == 0 else "홀수"     # 삼항 연산자
```

**거짓으로 취급되는 값**

`0` `0.0` `""` `[]` `{}` `()` `None` `False`

```python
if not lst:            # 리스트가 비었으면
    print("비어있음")
```

---

## 7. 반복문

### for

```python
for i in range(5):          # 0 1 2 3 4
for i in range(1, 6):       # 1 2 3 4 5
for i in range(0, 10, 2):   # 0 2 4 6 8
for i in range(5, 0, -1):   # 5 4 3 2 1

for item in [10, 20, 30]:   # 리스트 순회
    print(item)

for ch in "abc":            # 문자열 순회
    print(ch)
```

**같이 쓰면 편한 것**

```python
for i, v in enumerate(lst):      # 번호와 값을 동시에
    print(i, v)

for a, b in zip(names, ages):    # 두 리스트를 동시에
    print(a, b)

for k, v in d.items():           # 딕셔너리 키와 값
    print(k, v)
```

### while

```python
while x < 10:
    x += 1

while True:
    if 끝조건:
        break
```

### 반복 제어

```python
break       # 반복 즉시 중단
continue    # 이번 회차 건너뛰고 다음으로
pass        # 아무것도 안 함 (자리만 채움)
```

### 이중 반복문

```python
for i in range(1, 4):
    for j in range(1, 4):
        print(i, j)
```

---

## 8. 함수

```python
def add(a, b):
    return a + b

add(1, 2)                   # 3
```

```python
def hello(name="손님"):       # 기본값
    print(f"안녕 {name}")

hello()                     # 안녕 손님
hello("김")                  # 안녕 김
hello(name="김")             # 이름 지정해서 전달
```

```python
def minmax(lst):            # 여러 값 반환
    return min(lst), max(lst)

lo, hi = minmax([3, 1, 2])
```

```python
def total(*nums):           # 인자 개수 제한 없이
    return sum(nums)

total(1, 2, 3, 4)           # 10
```

```python
square = lambda x: x ** 2   # 람다 (한 줄짜리 함수)
square(3)                   # 9
```

**변수 범위**

```python
count = 0

def up():
    global count            # 함수 밖 변수를 바꾸려면 global
    count += 1
```

---

## 9. 예외 처리

```python
try:
    x = int(input())
except ValueError:
    print("숫자가 아닙니다")
except Exception as e:
    print("에러:", e)
else:
    print("에러 없을 때만 실행")
finally:
    print("무조건 실행")
```

**자주 보는 에러**

| 에러 | 원인 |
|---|---|
| `SyntaxError` | 문법 오류 (콜론·괄호 빠짐) |
| `IndentationError` | 들여쓰기 잘못됨 |
| `NameError` | 없는 변수 사용 |
| `TypeError` | 타입이 안 맞음 (`"1" + 1`) |
| `ValueError` | 값이 잘못됨 (`int("abc")`) |
| `IndexError` | 리스트 범위 벗어남 |
| `KeyError` | 딕셔너리에 없는 키 |
| `ZeroDivisionError` | 0으로 나눔 |

---

## 10. 파일 입출력

```python
# 쓰기
with open("a.txt", "w", encoding="utf-8") as f:
    f.write("안녕\n")

# 읽기
with open("a.txt", "r", encoding="utf-8") as f:
    text = f.read()             # 전체를 문자열로

with open("a.txt", encoding="utf-8") as f:
    for line in f:              # 한 줄씩
        print(line.strip())
```

**모드**

| 모드 | 뜻 |
|---|---|
| `"r"` | 읽기 (기본) |
| `"w"` | 새로 쓰기 (기존 내용 삭제) |
| `"a"` | 이어 쓰기 |

---

## 11. 모듈

```python
import random
import math

random.randint(1, 10)       # 1~10 중 랜덤
math.sqrt(16)               # 4.0

from math import sqrt       # 함수만 가져오기
sqrt(16)

import datetime as dt       # 별명 붙이기
dt.datetime.now()
```

**자주 쓰는 표준 라이브러리**

`random` 난수 · `math` 수학 · `datetime` 날짜 · `os` 파일/폴더 · `json` JSON · `re` 정규식 · `time` 시간

---

## 12. 클래스

```python
class Person:
    def __init__(self, name, age):    # 생성자
        self.name = name
        self.age = age

    def hello(self):                  # 메소드 (첫 인자는 항상 self)
        print(f"안녕 {self.name}")

p = Person("김", 20)
p.hello()
print(p.name)
```

**상속**

```python
class Student(Person):
    def __init__(self, name, age, school):
        super().__init__(name, age)   # 부모 생성자 호출
        self.school = school
```

---

## 13. 주석과 진입점

```python
# 한 줄 주석

"""
여러 줄 주석
"""

if __name__ == "__main__":      # 이 파일을 직접 실행할 때만
    main()
```

---

## 14. 꼭 기억할 것

```python
7 / 2                 # 3.5 (정수 나눗셈 아님). 몫은 //
input()               # 항상 문자열. 계산하려면 int() 변환
True / False / None   # 첫 글자 대문자
and / or / not        # && || ! 아님
i += 1                # i++ 없음
들여쓰기               # 스페이스 4칸, 섞어 쓰면 에러
```